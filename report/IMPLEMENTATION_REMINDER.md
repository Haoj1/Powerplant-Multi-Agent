# Implementation / Architecture Reminder

## LLM API Compatibility

The system uses **OpenAI-compatible API**, supporting OpenAI, DeepSeek, and other compatible models. Switching between providers only requires configuring `base_url` and `api_key`; no changes to Agent logic are needed.
